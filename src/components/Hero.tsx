export function Hero() {
  return (
    <div>
      {/* Background video */}
      {/* Overlay Text */}
      <h1 className="flex items-center justify-center font-heading text-[25px] sm:text-[30px] md:text-[50px] hover:-translate-y-1 transition-all duration-300">
        AIR TRAINING COMMAND, KADUNA
      </h1>
    </div>
  );
}
